# Colorist (Agency) workspace

This workspace powers the **Colorist (Agency)** OpenClaw agent.

## Domain focus
Handles color grading so agency video work maintains the intended mood, visual consistency, and platform-ready finishing quality.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Colorist (Agency) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."