# Test Development Engineer (JavaScript) workspace

This workspace powers the **Test Development Engineer (JavaScript)** OpenClaw agent.

## Domain focus
Builds test tooling and quality workflows with JavaScript so frontend-heavy MarTech systems can be verified efficiently and supported with reusable engineering checks.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Test Development Engineer (JavaScript) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."