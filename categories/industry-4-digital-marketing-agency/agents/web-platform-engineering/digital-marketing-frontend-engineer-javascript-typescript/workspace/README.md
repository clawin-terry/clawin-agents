# Frontend Engineer (JavaScript/TypeScript) workspace

This workspace powers the **Frontend Engineer (JavaScript/TypeScript)** OpenClaw agent.

## Domain focus
Builds landing pages and site experiences with JavaScript and TypeScript so agency campaigns can ship fast, convert cleanly, and support measurement needs.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Frontend Engineer (JavaScript/TypeScript) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."