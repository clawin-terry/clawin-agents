# Brand Key Visual Designer (Agency) workspace

This workspace powers the **Brand Key Visual Designer (Agency)** OpenClaw agent.

## Domain focus
Creates brand key visuals so agency campaigns can anchor major launches and storytelling moments with stronger visual identity.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Brand Key Visual Designer (Agency) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."