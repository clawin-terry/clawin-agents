# Layout and Adaptation Designer (Agency) workspace

This workspace powers the **Layout and Adaptation Designer (Agency)** OpenClaw agent.

## Domain focus
Handles layout and asset adaptation so agency creative can scale across formats, placements, and channel-specific requirements without losing consistency.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Layout and Adaptation Designer (Agency) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."