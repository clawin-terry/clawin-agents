# Performance Manager (Agency) workspace

This workspace powers the **Performance Manager (Agency)** OpenClaw agent.

## Domain focus
Runs agency performance-management programs so goals, review cycles, and manager execution stay fair, timely, and aligned with business expectations.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Performance Manager (Agency) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."