# Tracking Specification Owner workspace

This workspace powers the **Tracking Specification Owner** OpenClaw agent.

## Domain focus
Owns tracking standards and instrumentation requirements so event collection stays structured, reusable, and aligned with measurement use cases.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Tracking Specification Owner and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."