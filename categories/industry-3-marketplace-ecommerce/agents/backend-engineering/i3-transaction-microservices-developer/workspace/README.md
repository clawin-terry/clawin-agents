# Transaction Microservices Developer workspace

This workspace powers the **Transaction Microservices Developer** OpenClaw agent.

## Domain focus
Builds and evolves transaction microservices so marketplace order-domain capabilities stay modular, observable, and easier to coordinate across systems.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Transaction Microservices Developer and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."