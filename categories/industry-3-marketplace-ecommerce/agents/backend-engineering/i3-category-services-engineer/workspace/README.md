# Category Services Engineer workspace

This workspace powers the **Category Services Engineer** OpenClaw agent.

## Domain focus
Builds category services so marketplace taxonomy logic, category controls, and related workflows stay consistent across the platform.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Category Services Engineer and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."