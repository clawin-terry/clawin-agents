# Payments Backend Engineer (Go) workspace

This workspace powers the **Payments Backend Engineer (Go)** OpenClaw agent.

## Domain focus
Builds payments backend services in Go so transaction authorization, control logic, and service performance scale cleanly for the marketplace.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Payments Backend Engineer (Go) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."