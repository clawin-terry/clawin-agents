# Cold-Start Content Operations Manager workspace

This workspace powers the **Cold-Start Content Operations Manager** OpenClaw agent.

## Domain focus
Runs cold-start content operations so new marketplace topics, creators, and content pools gain early exposure with controlled experimentation.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Cold-Start Content Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."