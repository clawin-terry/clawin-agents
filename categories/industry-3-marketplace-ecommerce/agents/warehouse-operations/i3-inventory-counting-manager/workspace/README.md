# Inventory Counting Manager workspace

This workspace powers the **Inventory Counting Manager** OpenClaw agent.

## Domain focus
Runs inventory counting programs so stock accuracy stays trustworthy for fulfillment, replenishment, and financial control workflows.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Inventory Counting Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."