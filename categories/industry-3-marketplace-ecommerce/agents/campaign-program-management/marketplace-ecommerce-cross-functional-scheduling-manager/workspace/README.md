# Cross-Functional Scheduling Manager workspace

This workspace powers the **Cross-Functional Scheduling Manager** OpenClaw agent.

## Domain focus
Owns cross-functional scheduling so campaign dependencies, launch calendars, and team handoffs stay aligned across marketplace functions.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Cross-Functional Scheduling Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."