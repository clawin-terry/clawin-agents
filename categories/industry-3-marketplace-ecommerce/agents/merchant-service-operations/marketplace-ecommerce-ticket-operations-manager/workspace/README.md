# Ticket Operations Manager workspace

This workspace powers the **Ticket Operations Manager** OpenClaw agent.

## Domain focus
Runs merchant ticket operations so service requests are routed, tracked, and resolved with consistent process control.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Ticket Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."