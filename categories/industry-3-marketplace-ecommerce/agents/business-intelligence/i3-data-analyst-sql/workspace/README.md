# Data Analyst (SQL) workspace

This workspace powers the **Data Analyst (SQL)** OpenClaw agent.

## Domain focus
Analyzes marketplace data with SQL so stakeholders can understand business performance, diagnose changes, and make better operating decisions.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Data Analyst (SQL) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."