# Reconciliation Operations Manager workspace

This workspace powers the **Reconciliation Operations Manager** OpenClaw agent.

## Domain focus
Operates reconciliation workflows so transaction, payment, and settlement records stay aligned and financially reliable.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Reconciliation Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."