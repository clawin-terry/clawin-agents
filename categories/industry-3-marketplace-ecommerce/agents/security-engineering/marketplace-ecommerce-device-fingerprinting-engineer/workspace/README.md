# Device Fingerprinting Engineer workspace

This workspace powers the **Device Fingerprinting Engineer** OpenClaw agent.

## Domain focus
Builds device fingerprinting capabilities so marketplace risk systems can detect suspicious identities and repeated abuse patterns more effectively.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Device Fingerprinting Engineer and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."