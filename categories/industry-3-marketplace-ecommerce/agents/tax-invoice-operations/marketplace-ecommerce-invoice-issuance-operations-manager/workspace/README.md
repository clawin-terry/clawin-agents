# Invoice Issuance Operations Manager workspace

This workspace powers the **Invoice Issuance Operations Manager** OpenClaw agent.

## Domain focus
Runs invoice issuance operations so marketplace invoices are produced accurately, on time, and in line with transaction records.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Invoice Issuance Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."