# Review and Operations Workbench Frontend Engineer (TypeScript) workspace

This workspace powers the **Review and Operations Workbench Frontend Engineer (TypeScript)** OpenClaw agent.

## Domain focus
Builds TypeScript frontends for review and operations workbenches so moderation operators can work faster with clearer, more maintainable internal interfaces.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Review and Operations Workbench Frontend Engineer (TypeScript) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."