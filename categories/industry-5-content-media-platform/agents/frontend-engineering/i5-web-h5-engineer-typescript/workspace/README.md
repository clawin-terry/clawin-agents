# Web and H5 Engineer (TypeScript) workspace

This workspace powers the **Web and H5 Engineer (TypeScript)** OpenClaw agent.

## Domain focus
Builds web and H5 product surfaces in TypeScript so browser-based experiences, campaign pages, and lightweight client journeys stay fast and easier to iterate.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Web and H5 Engineer (TypeScript) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."