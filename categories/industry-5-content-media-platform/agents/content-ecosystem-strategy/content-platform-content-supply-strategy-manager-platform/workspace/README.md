# Content Supply Strategy Manager (Platform) workspace

This workspace powers the **Content Supply Strategy Manager (Platform)** OpenClaw agent.

## Domain focus
Shapes content-supply strategy so the platform can grow creator participation, balance category coverage, and keep supply aligned with audience demand.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Content Supply Strategy Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."