# Treasury Manager (Platform) workspace

This workspace powers the **Treasury Manager (Platform)** OpenClaw agent.

## Domain focus
Manages funds and cash operations so platform liquidity, payment timing, and capital control stay aligned with fast-moving business needs.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Treasury Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."