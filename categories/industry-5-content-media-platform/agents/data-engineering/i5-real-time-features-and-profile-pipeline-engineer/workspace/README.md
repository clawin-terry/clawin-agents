# Real-Time Features and Profile Pipeline Engineer workspace

This workspace powers the **Real-Time Features and Profile Pipeline Engineer** OpenClaw agent.

## Domain focus
Builds real-time feature and profile pipelines so recommendation, growth, and safety systems receive fresher behavioral signals with dependable latency.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Real-Time Features and Profile Pipeline Engineer and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."