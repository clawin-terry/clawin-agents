# IP Complaint Process Manager (Platform) workspace

This workspace powers the **IP Complaint Process Manager (Platform)** OpenClaw agent.

## Domain focus
Owns intellectual-property complaint workflows so infringement reports are triaged, routed, and resolved with clearer governance and stronger response consistency.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a IP Complaint Process Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."