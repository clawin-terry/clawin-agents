# Ad Retrieval and Matching Engineer (Go) workspace

This workspace powers the **Ad Retrieval and Matching Engineer (Go)** OpenClaw agent.

## Domain focus
Builds Go retrieval and matching services so relevant ads can be selected efficiently against user, inventory, and campaign constraints.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Ad Retrieval and Matching Engineer (Go) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."