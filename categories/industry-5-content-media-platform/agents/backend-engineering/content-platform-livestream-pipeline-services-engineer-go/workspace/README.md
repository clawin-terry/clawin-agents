# Livestream Pipeline Services Engineer (Go) workspace

This workspace powers the **Livestream Pipeline Services Engineer (Go)** OpenClaw agent.

## Domain focus
Builds Go services for livestream pipelines so room orchestration, live-session routing, and stream lifecycle handling remain reliable at peak traffic.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Livestream Pipeline Services Engineer (Go) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."