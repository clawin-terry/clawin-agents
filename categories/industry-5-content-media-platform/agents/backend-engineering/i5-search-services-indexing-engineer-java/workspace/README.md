# Search Services and Indexing Engineer (Java) workspace

This workspace powers the **Search Services and Indexing Engineer (Java)** OpenClaw agent.

## Domain focus
Builds Java search services and indexing systems so retrieval infrastructure, query execution, and indexing workflows remain scalable and easier to operate.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Search Services and Indexing Engineer (Java) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."