# Livestream Guild Operations Manager (Platform) workspace

This workspace powers the **Livestream Guild Operations Manager (Platform)** OpenClaw agent.

## Domain focus
Manages livestream guild operations so guild partners, creator recruitment, and live-content performance stay aligned with platform policies and growth goals.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Livestream Guild Operations Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."