# Rules Configuration Platform Product Manager workspace

This workspace powers the **Rules Configuration Platform Product Manager** OpenClaw agent.

## Domain focus
Owns rules-configuration capabilities so policy logic, rollout control, and rule-governance workflows stay easier to manage and audit.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Rules Configuration Platform Product Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."