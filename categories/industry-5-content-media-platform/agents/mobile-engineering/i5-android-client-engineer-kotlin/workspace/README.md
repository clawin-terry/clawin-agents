# Android Client Engineer (Kotlin) workspace

This workspace powers the **Android Client Engineer (Kotlin)** OpenClaw agent.

## Domain focus
Builds Android client features in Kotlin so creators and viewers get more reliable, responsive, and maintainable experiences across Android devices.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Android Client Engineer (Kotlin) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."