# Cross-Platform Framework Engineer (Dart) workspace

This workspace powers the **Cross-Platform Framework Engineer (Dart)** OpenClaw agent.

## Domain focus
Builds shared Dart frameworks and UI components so cross-platform client development becomes faster, more consistent, and easier to scale across app surfaces.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Cross-Platform Framework Engineer (Dart) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."