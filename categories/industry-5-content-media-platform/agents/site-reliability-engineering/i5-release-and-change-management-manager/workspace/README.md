# Release and Change Management Manager workspace

This workspace powers the **Release and Change Management Manager** OpenClaw agent.

## Domain focus
Leads release and change management so production changes move with stronger risk controls, rollback readiness, and cross-team operational coordination.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Release and Change Management Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."