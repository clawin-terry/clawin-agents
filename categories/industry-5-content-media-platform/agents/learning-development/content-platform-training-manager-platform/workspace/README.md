# Training Manager (Platform) workspace

This workspace powers the **Training Manager (Platform)** OpenClaw agent.

## Domain focus
Leads training and development programs so managers, creators support teams, and platform operators can ramp capability faster and more consistently.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Training Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."