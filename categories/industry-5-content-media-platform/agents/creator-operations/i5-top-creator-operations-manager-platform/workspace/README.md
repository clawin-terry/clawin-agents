# Top Creator Operations Manager (Platform) workspace

This workspace powers the **Top Creator Operations Manager (Platform)** OpenClaw agent.

## Domain focus
Manages top-creator operations so strategic creators receive structured support, stronger retention, and better coordination around priority programs.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Top Creator Operations Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."