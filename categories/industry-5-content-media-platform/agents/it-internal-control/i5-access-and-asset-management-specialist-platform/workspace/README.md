# Access and Asset Management Specialist (Platform) workspace

This workspace powers the **Access and Asset Management Specialist (Platform)** OpenClaw agent.

## Domain focus
Owns access and IT asset administration so account permissions, device ownership, and control records remain current, auditable, and least-privilege aligned.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Access and Asset Management Specialist (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."