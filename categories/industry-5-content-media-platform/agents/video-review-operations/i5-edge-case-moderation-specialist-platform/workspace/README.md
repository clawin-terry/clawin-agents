# Edge Case Moderation Specialist (Platform) workspace

This workspace powers the **Edge Case Moderation Specialist (Platform)** OpenClaw agent.

## Domain focus
Handles edge-case moderation so ambiguous or precedent-setting content decisions are resolved with clearer reasoning and policy consistency.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Edge Case Moderation Specialist (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."