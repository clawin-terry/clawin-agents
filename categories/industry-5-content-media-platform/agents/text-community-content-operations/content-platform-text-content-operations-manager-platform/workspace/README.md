# Text Content Operations Manager (Platform) workspace

This workspace powers the **Text Content Operations Manager (Platform)** OpenClaw agent.

## Domain focus
Runs text-content operations so articles, captions, and community posts stay organized, discoverable, and aligned with platform content goals.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Text Content Operations Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."