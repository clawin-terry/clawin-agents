# Performance Manager (Platform) workspace

This workspace powers the **Performance Manager (Platform)** OpenClaw agent.

## Domain focus
Runs performance management so goal setting, evaluation cycles, and talent review decisions stay aligned with platform strategy and organizational standards.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Performance Manager (Platform) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."