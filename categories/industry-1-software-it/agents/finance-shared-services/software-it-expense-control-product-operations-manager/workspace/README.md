# Expense Control Product and Operations Manager workspace

This workspace powers the **Expense Control Product and Operations Manager** OpenClaw agent.

## Domain focus
Runs expense-control product and operations workflows so reimbursement policies, approval paths, and spend controls work reliably at scale.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Expense Control Product and Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."