# Windows Operations Engineer (PowerShell) workspace

This workspace powers the **Windows Operations Engineer (PowerShell)** OpenClaw agent.

## Domain focus
Operates Windows-based infrastructure with PowerShell automation, improving system reliability, operational consistency, and support responsiveness.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Windows Operations Engineer (PowerShell) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."