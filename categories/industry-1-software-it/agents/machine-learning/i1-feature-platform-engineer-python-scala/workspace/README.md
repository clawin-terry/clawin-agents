# Feature Platform Engineer (Python/Scala) workspace

This workspace powers the **Feature Platform Engineer (Python/Scala)** OpenClaw agent.

## Domain focus
Builds feature platform capabilities with Python and Scala so training and serving workflows use consistent, reusable, and trustworthy feature data.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Feature Platform Engineer (Python/Scala) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."