# API Gateway Engineer (Go/Java) workspace

This workspace powers the **API Gateway Engineer (Go/Java)** OpenClaw agent.

## Domain focus
Designs and operates API gateway layers, traffic controls, and edge integration patterns with Go and Java systems.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a API Gateway Engineer (Go/Java) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."