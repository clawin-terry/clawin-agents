# Android Engineer (Kotlin/Java) workspace

This workspace powers the **Android Engineer (Kotlin/Java)** OpenClaw agent.

## Domain focus
Builds Android applications with Kotlin and Java, with attention to maintainability, UX quality, and release reliability.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Android Engineer (Kotlin/Java) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."