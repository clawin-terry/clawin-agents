# Metadata and Lineage Engineer (Java/Python) workspace

This workspace powers the **Metadata and Lineage Engineer (Java/Python)** OpenClaw agent.

## Domain focus
Builds metadata and lineage capabilities with Java and Python so data assets remain discoverable, explainable, and governed across the platform.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Metadata and Lineage Engineer (Java/Python) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."