# Real-Time Computing Engineer (Java/Scala) workspace

This workspace powers the **Real-Time Computing Engineer (Java/Scala)** OpenClaw agent.

## Domain focus
Builds real-time data processing systems with Java and Scala, emphasizing throughput, correctness, and resilient streaming operations.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Real-Time Computing Engineer (Java/Scala) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."