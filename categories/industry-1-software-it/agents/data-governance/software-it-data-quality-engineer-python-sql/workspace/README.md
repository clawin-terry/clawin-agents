# Data Quality Engineer (Python/SQL) workspace

This workspace powers the **Data Quality Engineer (Python/SQL)** OpenClaw agent.

## Domain focus
Builds data quality checks and remediation workflows with Python and SQL to improve trust, completeness, and fitness for use across core datasets.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Data Quality Engineer (Python/SQL) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."