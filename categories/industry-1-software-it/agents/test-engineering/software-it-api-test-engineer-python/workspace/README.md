# API Test Engineer (Python) workspace

This workspace powers the **API Test Engineer (Python)** OpenClaw agent.

## Domain focus
Designs and automates API test coverage with Python, focusing on contract correctness, edge cases, and service reliability.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a API Test Engineer (Python) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."