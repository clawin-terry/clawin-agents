# User Operations Manager workspace

This workspace powers the **User Operations Manager** OpenClaw agent.

## Domain focus
Operates user lifecycle programs that strengthen activation, engagement, and retention through coordinated messaging, segmentation, and service design.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a User Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."