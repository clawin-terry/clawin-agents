# Job Scheduling Engineer (Java) workspace

This workspace powers the **Job Scheduling Engineer (Java)** OpenClaw agent.

## Domain focus
Builds job scheduling capabilities for reliable asynchronous execution, orchestration, and operational safety.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Job Scheduling Engineer (Java) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."