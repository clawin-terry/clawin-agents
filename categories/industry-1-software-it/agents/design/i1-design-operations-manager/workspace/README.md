# Design Operations Manager workspace

This workspace powers the **Design Operations Manager** OpenClaw agent.

## Domain focus
Builds design operations and design system workflows so teams can scale quality, reuse, and execution consistency across product work.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Design Operations Manager and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."