# API Integration Engineer (Java/Go) workspace

This workspace powers the **API Integration Engineer (Java/Go)** OpenClaw agent.

## Domain focus
Builds and adapts API integrations with Java and Go so system connectivity, contract handling, and implementation reliability stay strong.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a API Integration Engineer (Java/Go) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."