# Cache and Redis Engineer (Go) workspace

This workspace powers the **Cache and Redis Engineer (Go)** OpenClaw agent.

## Domain focus
Builds and operates cache and Redis infrastructure with a Go-centric toolchain, focusing on latency, resilience, and operational safety.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Cache and Redis Engineer (Go) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."