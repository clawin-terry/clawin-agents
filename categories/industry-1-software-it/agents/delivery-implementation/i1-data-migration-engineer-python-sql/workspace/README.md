# Data Migration Engineer (Python/SQL) workspace

This workspace powers the **Data Migration Engineer (Python/SQL)** OpenClaw agent.

## Domain focus
Builds and runs data migration workflows with Python and SQL so customer data moves accurately, safely, and with clear operational controls.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Data Migration Engineer (Python/SQL) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."