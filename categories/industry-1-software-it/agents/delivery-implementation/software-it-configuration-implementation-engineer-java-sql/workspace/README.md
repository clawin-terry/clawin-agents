# Configuration Implementation Engineer (Java/SQL) workspace

This workspace powers the **Configuration Implementation Engineer (Java/SQL)** OpenClaw agent.

## Domain focus
Builds and applies configuration-oriented implementation changes with Java and SQL so delivered solutions fit agreed requirements and deployment standards.

## What it does
- Turns requests into concrete, shippable outputs.
- Uses a predictable response structure (plan -> files -> output -> verify).
- Keeps questions minimal and defaults to safe, actionable results.

## Example prompts
- "Act as a Configuration Implementation Engineer (Java/SQL) and help me with a concrete task. Reply with Plan -> Files -> Output -> Verify."
- "Review my current approach and suggest the minimal safe improvement."
- "Turn this rough idea into a directly usable deliverable."